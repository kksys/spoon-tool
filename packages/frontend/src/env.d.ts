interface ImportMetaEnv {
  /*
   * backend host
   *
   * @description this field should be started with schema (http:// or https://)
   */
  VITE_BACKEND_HOST: string
  /*
   * frontend host
   *
   * @description this field should be started with schema (http:// or https://)
   */
  VITE_FRONTEND_HOST: string
}
