{
  $schema: "https://docs.renovatebot.com/renovate-schema.json",
  extends: [
    "config:recommended",
    "docker:pinDigests",
    "helpers:pinGitHubActionDigests",
    ":disableRateLimiting"
  ],
  enabledManagers: [
    "devcontainer",
    "github-actions",
    "custom.regex",
    "npm",
  ],
  vulnerabilityAlerts: {
    labels: [
      "security"
    ],
  },
  hostRules: [
    {
      abortOnError: true,
    }
  ],
  assigneesSampleSize: 2,
  reviewersSampleSize: 1,
  timezone: "Asia/Tokyo",
  schedule: [
    "before 2am on Sunday"
  ],
  customManagers: [
    {
      customType: "regex",
      fileMatch: [
        "^pnpm-workspace.yaml$"
      ],
      matchStringsStrategy: "any",
      matchStrings: [
        "  (?<quote>\"?)(?<depName>[^:\n\"]+)\"?: (?<currentValue>[^\n]+)"
      ],
      autoReplaceStringTemplate: "  {{{quote}}}{{{depName}}}{{{quote}}}: {{{newValue}}}",
      datasourceTemplate: "npm",
    },
  ],
  packageRules: [
    {
      matchManagers: [
        "custom.regex",
        "npm",
      ],
    },
  ],
}
